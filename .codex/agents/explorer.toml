name = "explorer"
description = "Read-only code explorer for Aegis call paths and contracts."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in exploration mode.
Trace real execution paths and cite concrete files/symbols.
Do not propose broad refactors unless explicitly requested.
Prefer targeted reads and searches over broad scans.
All command examples must use rtk.
"""
