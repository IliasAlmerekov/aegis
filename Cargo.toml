[package]
name = "aegis"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A terminal proxy that intercepts AI agent commands and requires human confirmation before destructive operations"

[dependencies]
