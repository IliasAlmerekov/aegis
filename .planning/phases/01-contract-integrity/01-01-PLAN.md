---
phase: 01-contract-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/full_pipeline.rs
autonomous: true
requirements:
  - T1.3-GIT-OFF
  - T1.3-DOCKER-OFF
user_setup: []
must_haves:
  truths:
    - "With `auto_snapshot_git = false`, the real `aegis` binary produces a Danger-path audit entry whose `snapshots` field is `[]` and never invokes `git`."
    - "With `auto_snapshot_docker = false`, the real `aegis` binary produces a Danger-path audit entry whose `snapshots` field is `[]` and never invokes `docker`."
    - "Phase 01.3 adds executable regression coverage without changing already-correct production wiring unless the new tests prove otherwise."
  artifacts:
    - path: tests/full_pipeline.rs
      provides: "Shared stub-log reader plus git-off and docker-off subprocess regressions"
      contains:
        - "snapshot_registry_git_flag_false_skips_plugin_and_audit"
        - "snapshot_registry_docker_flag_false_skips_plugin_and_audit"
  key_links:
    - from: tests/full_pipeline.rs
      to: src/config/model.rs
      via: "workspace `.aegis.toml` loaded by the real binary through `current_dir(workspace.path())`"
      pattern: "auto_snapshot_git = false|auto_snapshot_docker = false"
    - from: tests/full_pipeline.rs
      to: src/runtime.rs
      via: "real subprocess path through `base_command(home.path())` and `RuntimeContext::new`"
      pattern: "CARGO_BIN_EXE_aegis|base_command"
    - from: tests/full_pipeline.rs
      to: src/audit/logger.rs
      via: "audit JSONL assertion through `read_audit_entries(home)`"
      pattern: "entries\\[0\\]\\[\"snapshots\"\\]"
---

<objective>
Add subprocess regression coverage proving SnapshotRegistry config flags are honored at the real-binary boundary for Ticket 1.3.

Purpose: Close the end-to-end contract gap identified in D-04/D-05/D-06 without touching production snapshot, runtime, UI, or audit wiring unless the new regressions disprove D-02/D-03.
Output: One focused test-only diff in `tests/full_pipeline.rs` containing a shared stub-log helper and two new negative-path regressions.
</objective>

<execution_context>
@/home/iliasalmerekov/Projects/aegis/.claude/get-shit-done/workflows/execute-plan.md
@/home/iliasalmerekov/Projects/aegis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contract-integrity/1.3-CONTEXT.md
@.planning/phases/01-contract-integrity/01-RESEARCH.md
@.planning/phases/01-contract-integrity/01-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@tests/full_pipeline.rs
@src/snapshot/mod.rs
@src/runtime.rs
@src/config/model.rs
@src/main.rs
@src/audit/logger.rs
@src/ui/confirm.rs

<interfaces>
From `tests/full_pipeline.rs`:
```rust
fn base_command(home: &Path) -> Command
fn read_audit_entries(home: &Path) -> Vec<Value>
fn write_executable(path: &Path, body: &str)
```

From `src/snapshot/mod.rs`:
```rust
pub fn from_config(config: &Config) -> Self
pub async fn snapshot_all(&self, cwd: &Path, cmd: &str) -> Vec<SnapshotRecord>
```

From `src/runtime.rs`:
```rust
pub fn new(config: Config) -> Self
pub fn create_snapshots(&self, cwd: &Path, cmd: &str, verbose: bool) -> Vec<SnapshotRecord>
pub fn append_audit_entry(...)
```

From `src/config/model.rs` defaults:
```rust
auto_snapshot_git: true
auto_snapshot_docker: false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared stub-log helper and git-off real-binary regression</name>
  <files>tests/full_pipeline.rs</files>
  <read_first>
    - tests/full_pipeline.rs
    - .planning/phases/01-contract-integrity/1.3-CONTEXT.md
    - .planning/phases/01-contract-integrity/01-RESEARCH.md
    - .planning/phases/01-contract-integrity/01-VALIDATION.md
    - src/snapshot/mod.rs
    - src/runtime.rs
    - src/config/model.rs
    - src/main.rs
    - src/audit/logger.rs
  </read_first>
  <action>
    In `tests/full_pipeline.rs`, first add one tiny helper named `read_stub_invocations(log_path: &Path) -> Vec<String>` next to `read_audit_entries` so the new subprocess regressions can assert PATH stubs were never called. Then add `#[test] fn snapshot_registry_git_flag_false_skips_plugin_and_audit()` per D-04, D-05, and D-06. Use a fresh `TempDir` for `home`, a second fresh non-git `TempDir` for `workspace`, and a `workspace/bin/git` stub written with `write_executable` that appends `"$*"` to `AEGIS_TEST_GIT_LOG`. Write an explicit workspace `.aegis.toml` that sets both `auto_snapshot_git = false` and `auto_snapshot_docker = false` so the assertion isolates the git flag. Run the real compiled binary via `base_command(home.path())`, set `current_dir(workspace.path())`, prepend the stub bin dir to `PATH`, set `AEGIS_FORCE_INTERACTIVE=1`, execute a Danger command such as `rm -rf {sentinel_path}`, pipe `no\n`, and assert: exit code is `2`, the git stub log is absent or empty, audit entry count is `1`, `entries[0]["decision"] == "Denied"`, `entries[0]["risk"] == "Danger"`, and `entries[0]["snapshots"] == serde_json::json!([])`. Do not change `src/snapshot/mod.rs`, `src/runtime.rs`, `src/ui/confirm.rs`, or `src/audit/logger.rs` unless this regression proves D-02 or D-03 false.
  </action>
  <acceptance_criteria>
    - `rtk rg "fn read_stub_invocations\\(" tests/full_pipeline.rs` finds the shared helper.
    - `rtk rg "fn snapshot_registry_git_flag_false_skips_plugin_and_audit\\(" tests/full_pipeline.rs` finds the new git-off regression.
    - `rtk rg "AEGIS_TEST_GIT_LOG" tests/full_pipeline.rs` finds the git stub wiring.
    - `rtk rg "auto_snapshot_git = false" tests/full_pipeline.rs` finds the explicit git-off fixture config.
    - `rtk rg "entries\\[0\\]\\[\\\"snapshots\\\"\\], serde_json::json!\\(\\[\\]\\)" tests/full_pipeline.rs` finds the empty-snapshots assertion.
  </acceptance_criteria>
  <verify>
    <automated>rtk cargo test --test full_pipeline snapshot_registry_git_flag_false_skips_plugin_and_audit</automated>
  </verify>
  <done>The git-off regression fails if the real binary still registers or probes the Git snapshot plugin when `auto_snapshot_git = false`, and it passes only when no git invocation occurs and the audit `snapshots` array is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Add the docker-off real-binary regression using the same helper pattern</name>
  <files>tests/full_pipeline.rs</files>
  <read_first>
    - tests/full_pipeline.rs
    - .planning/phases/01-contract-integrity/1.3-CONTEXT.md
    - .planning/phases/01-contract-integrity/01-RESEARCH.md
    - .planning/phases/01-contract-integrity/01-VALIDATION.md
    - src/snapshot/mod.rs
    - src/runtime.rs
    - src/config/model.rs
    - src/main.rs
    - src/audit/logger.rs
  </read_first>
  <action>
    In the same file, add `#[test] fn snapshot_registry_docker_flag_false_skips_plugin_and_audit()` per D-04 and the research correction that “no docker invocation at all” is the contract, not merely “no docker commit”. Reuse `read_stub_invocations(...)`, `base_command(...)`, and `write_executable(...)`. Create a temp `home`, temp `workspace`, and `workspace/bin/docker` stub that appends `"$*"` to `AEGIS_TEST_DOCKER_LOG`. Write an explicit `.aegis.toml` containing `auto_snapshot_git = false` and `auto_snapshot_docker = false` so git cannot add unrelated snapshot noise. Run the real binary from that workspace with `AEGIS_FORCE_INTERACTIVE=1`, pipe `no\n` to a Danger command, and assert exit code `2`, the docker stub log is absent or empty, the audit contains exactly one entry, `entries[0]["decision"] == "Denied"`, `entries[0]["risk"] == "Danger"`, and `entries[0]["snapshots"] == serde_json::json!([])`. Keep the production code untouched unless this regression proves the existing wiring is wrong.
  </action>
  <acceptance_criteria>
    - `rtk rg "fn snapshot_registry_docker_flag_false_skips_plugin_and_audit\\(" tests/full_pipeline.rs` finds the new docker-off regression.
    - `rtk rg "AEGIS_TEST_DOCKER_LOG" tests/full_pipeline.rs` finds the docker stub wiring.
    - `rtk rg "auto_snapshot_docker = false" tests/full_pipeline.rs` finds the explicit docker-off fixture config.
    - `rtk rg "entries\\[0\\]\\[\\\"decision\\\"\\], \\\"Denied\\\"" tests/full_pipeline.rs` finds audit decision assertions in the new regressions.
    - `rtk rg "entries\\[0\\]\\[\\\"snapshots\\\"\\], serde_json::json!\\(\\[\\]\\)" tests/full_pipeline.rs` still matches the empty-snapshots assertion used by both regressions.
  </acceptance_criteria>
  <verify>
    <automated>rtk cargo test --test full_pipeline snapshot_registry_docker_flag_false_skips_plugin_and_audit</automated>
  </verify>
  <done>The docker-off regression fails if the real binary still registers or probes the Docker snapshot plugin when `auto_snapshot_docker = false`, and it passes only when no docker invocation occurs and the audit `snapshots` array is empty.</done>
</task>

</tasks>

<verification>
- Targeted checks:
  - `rtk cargo test --test full_pipeline snapshot_registry_git_flag_false_skips_plugin_and_audit`
  - `rtk cargo test --test full_pipeline snapshot_registry_docker_flag_false_skips_plugin_and_audit`
- Wave check:
  - `rtk cargo test --test full_pipeline snapshot_`
- If the executor had to touch any production file because a regression exposed a real bug, run the relevant verification-loop subset before closing the plan:
  - `rtk cargo fmt --check`
  - `rtk cargo clippy -- -D warnings`
  - `rtk cargo test`
</verification>

<success_criteria>
- `tests/full_pipeline.rs` is the only required file change for the planned path.
- Both new test names exist exactly as specified and assert both “no stub invocation” and `snapshots == []`.
- The plan preserves D-01 through D-03 by default: no production snapshot/runtime/UI/audit edits unless test evidence forces them.
- Phase 01 gains executable regression coverage for both config flags at the subprocess boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-integrity/01-01-SUMMARY.md`.
</output>
